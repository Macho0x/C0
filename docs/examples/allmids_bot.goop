module Trading.AllMidsBot

type symbol = newtype string

let midPrice (sym: symbol) : float =
  100.0

let decide (px: float) : string =
  if px > 100.0 then "sell" else "buy"

let main () : unit with { io } =
  let sym = Symbol "BTC-USD" in
  let px = midPrice sym in
  print_line (decide px)
