module main

type Resource = { name: string }

let Close (r: Resource) : unit =
  print_line ("closing " ^ r.name)

let main () =
  let r = { name = "demo" } in
  begin
    print_line "using resource";
    Close r
  end
