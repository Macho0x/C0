module Trading.Position

let transfer (ch: int owned_chan) (value: int) : unit with { async } =
  let u = OwnedChan.send ch value in
  ()

let main () : unit with { async } =
  let ch = OwnedChan.make () in
  transfer ch 42
