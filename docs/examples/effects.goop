module main

(* Explicit IO effect — annotations deferred; effects via handlers later *)
let readFile (path: string) : string =
  (* would call extern Go file reading *)
  path

(* Pure function *)
let double (x: int) : int = x * 2

(* Would log under a log effect handler *)
let logAndReturn (x: int) : int =
  (* would log something *)
  x

(* Pure main *)
let main () =
  let n = double 21 in
  let m = double (double 1) in
  print_line (int_to_string (n + m))
