module Trading.SimpleBot

(* DESIGN SKETCH — full bot logic is documented in git history and trading docs.
   This stub verifies the module path and naming conventions compile. *)

let main () =
  print_line "Trading.SimpleBot design sketch"
