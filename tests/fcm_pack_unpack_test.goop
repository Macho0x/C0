module FcmPackUnpackTest

module type S = sig
  val answer : int
end

module M : S = struct
  let answer : int = 42
end

let packed = (module M : S)
let unpacked = (val packed : S)

open M

let main () =
  assert (answer = 42)
