module TradingBrandedIdsTest

type order_id = | Order_id of string
type symbol = | Symbol of string

let oid = Order_id "ord-42"
let sym = Symbol "BTC-USD"

let useIds (id: order_id) (s: symbol) : unit =
  print_line "branded ids ok"

let main () : unit =
  useIds oid sym
